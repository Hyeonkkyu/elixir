<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.elixir.mvc.dao.UserDAO">

    <select id="getUser" parameterType="java.lang.String" resultType="com.elixir.mvc.commons.UserVO">
        SELECT * FROM user_detail WHERE user_code = 1;
    </select>

</mapper>